[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
markers =
    slow: corridas de entrenamiento completas (minutos)
    data: requiere data/raw/supermarket_products.csv, que no se versiona
